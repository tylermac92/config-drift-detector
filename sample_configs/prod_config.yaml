# Production Configuration
server:
  host: "0.0.0.0"
  port: 8080
  debug: false

database:
  host: "prod-db.example.com"
  port: 5432
  name: "myapp_prod"
  ssl: true

logging:
  level: "INFO"
  format: "json"

features:
  new_feature: true
  beta_feature: false